version: 2

macros:
  - name: rolling_aggregation_n_periods
    description: "{{ doc('macro__rolling_aggregation_n_periods') }}"
    arguments:
      - name: column_name
        type: string
        description: "{{ doc('macro__rolling_aggregation_n_periods__column_name') }}"
      - name: partition_by
        type: string
        description: "{{ doc('macro__rolling_aggregation_n_periods__partition_by') }}"
      - name: number_of_periods
        type: INT
        description: "{{ doc('macro__rolling_aggregation_n_periods__number_of_periods') }}"
      - name: aggregation_type
        type: string
        description: "{{ doc('macro__rolling_aggregation_n_periods__aggregation_type') }}"
      - name: order_by
        type: string
        description: "{{ doc('macro__rolling_aggregation_n_periods__order_by') }}"
