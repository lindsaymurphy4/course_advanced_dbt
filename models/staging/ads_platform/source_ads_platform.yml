sources:
  - name: ads_platform
    schema: public
    database: raw
    tables:
      - name: daily_ads
        identifier: ads
        description: "Daily ad spend data at the campaign level. Downstream we use [date, campaign-id] as a surrogate key."
