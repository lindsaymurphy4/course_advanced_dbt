sources:
  - name: ads_platform
    schema: bingeflix
    description: This source contains ads platform data.
    database: raw
    tables:
      - name: daily_ads
        identifier: ads
        description: '{{ doc("bingeflix_source_table_daily_ads") }}'
