models:
  - name: mrr
    config:
      tags: p0
    description: '{{ docs("marts__finance__table__mrr") }}'
      
    columns:
      - name: surrogate_key
        description: '{{ docs("marts__finance__mrr__column__surrogate_key") }}'
        tests:
          - not_null
          - unique:
              config:
                tags: p0

      - name: date_month
        description: 

      - name: user_id
        description: '{{ doc("bingeflix__users__column__user_id") }}'

      - name: subscription_id
        description: '{{ doc("bingeflix__subscriptions__column__subscription_id") }}'

      - name: starts_at
        description: '{{ docs("bingeflix__subscriptions__column__starts_at") }}'

      - name: ends_at
        description: '{{ docs("bingeflix__subscriptions__column__ends_at") }}'

      - name: plan_name
        description: '{{ docs("bingeflix__subscription_plans__column__plan_name") }}'

      - name: mrr_amount
        description: '{{ docs("marts__finance__mrr__column__mrr_amount") }}'

      - name: mrr_change
        description: '{{ docs("marts__finance__mrr__column__mrr_change") }}'

      - name: retained_mrr_amount
        description: '{{ docs("marts__finance__mrr__column__retained_mrr_amount") }}'

      - name: previous_month_mrr_amount
        description: '{{ docs("marts__finance__mrr__column__previous_month_mrr_amount") }}'

      - name: change_category
        description: '{{ docs("marts__finance__mrr__column__change_category") }}'

      - name: month_retained_number
        description: '{{ docs("marts__finance__mrr__column__month_retained_number") }}'