models:
  - name: fct_events
    description: This model contains information about Bingeflix users.
    meta:
      priority: high
      owner:
        - lindsay
    columns:
      - name: event_id
        description: '{{ doc("bingeflix_column_event_id") }}'
        data_tests:
          - not_null
          - unique

      - name: session_id
        description: '{{ doc("bingeflix_column__events__session_id") }}'
        data_tests:
          - not_null

      - name: created_at
        description: '{{ doc("bingeflix_column__events__created_at") }}'
        data_tests:
          - not_null

      - name: user_id
        description: '{{ doc("bingeflix_column_user_id") }}'
        data_tests:
          - not_null

      - name: event_name
        description: '{{ doc("bingeflix_column_event_name") }}'
        data_tests:
          - not_null
