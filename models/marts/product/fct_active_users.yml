models:
  - name: fct_active_users
    description: '{{ docs("marts__product__table__fct_active_users") }}'
    columns:
      - name: surrogate_key
        description: '{{ docs("marts__prodict__fct_active_users__column__surrogate_key") }}'
        tests:
          - not_null
          - unique

      - name: DATE_WEEK
        description: '{{ docs("int__int_dates__column__date_week") }}'

      - name: USER_ID
        description: '{{ docs("bingeflix__users__column__user_id") }}'
        
      - name: LOGIN_COUNT
        description: '{{ docs("marts__prodict__fct_active_users__column__login_count") }}'